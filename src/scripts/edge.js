class Edge{
    constructor(node_instance,path_start) {
        this.startNode = node_instance;
        //startNode is the clicked on node that initiates the line
        this.startPos = path_start;
        //startPos is the path or line object
    }
    createPath(){   
    }
}
export default Edge;